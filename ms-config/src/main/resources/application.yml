server:
  port: 8888

spring:
  application:
    name: ms-config
    title: MsConfigServer
    version: 0.0.1
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
